# Copy this file to .env.local and fill in the values after deploying the backend.
# Get the API URL from `sam deploy` output (ApiUrl).
NEXT_PUBLIC_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod
